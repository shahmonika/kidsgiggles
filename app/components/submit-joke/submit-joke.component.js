import { Component } from '@angular/core';
import '../../rxjs-operators';
export var SubmitJokeComponent = (function () {
    function SubmitJokeComponent() {
    }
    // @Input()
    // joke: Joke;
    SubmitJokeComponent.decorators = [
        { type: Component, args: [{
                    moduleId: module.id,
                    selector: 'submit-joke',
                    templateUrl: 'submit-joke.component.html'
                },] },
    ];
    /** @nocollapse */
    SubmitJokeComponent.ctorParameters = [];
    return SubmitJokeComponent;
}());
